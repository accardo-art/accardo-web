version: 2

workflows:
  version: 2

  workflow:
    jobs:
      - compile
                  
jobs:
  compile:
    machine: true
    
    steps:
      - checkout

      - run:
          name: Compile Jekyll Site
          command: ls -al && docker-compose run --rm build